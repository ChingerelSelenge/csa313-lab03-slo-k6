# Лаб №3

**Нэр:** С.Чингэрэл
**Оюутны код:** B232270010

## k6 хувилбар

```
$ k6 v2.2.0
```

---

## Хийсэн ажил

`server.js`-д гурван endpoint-той локал API (`/cart/add`, `/report`, `/pay`) босгож, тус бүрд чанарын сценарио бичээд, SLO болгож, `slo-test.js`-д k6 threshold болгож кодчиллоо. Дараа нь chaos туршилт хийж, threshold-оо зориуд эвдэж баталгаажуулав.

---

## Сценарионууд

| Сценарио                        | Тойм                                  | Системийн төлөв                | Орчны төлөв                  | Гадаад өдөөлт                               | Шаардлагатай хариу                       | Хэмжүүр                                                                     |
| ------------------------------- | ------------------------------------- | ------------------------------ | ---------------------------- | ------------------------------------------- | ---------------------------------------- | --------------------------------------------------------------------------- |
| Performance — `/cart/add`       | Сагсанд нэмэх хүсэлтийн хариу хугацаа | Сервер хэвийн ажиллаж байна    | Локал сервер, 20 VU, 1 минут | Хэрэглэгч `POST /cart/add` илгээнэ          | `200 OK` буцаана                         | Хариу хугацааны p95 < 5 мс                                                  |
| Reliability — `/pay`            | Төлбөрийн хүсэлтийн алдааны давтамж   | Сервер хэвийн ажиллаж байна    | Локал сервер, 20 VU, 1 минут | Хэрэглэгч `POST /pay` илгээнэ               | Амжилттай үед `200 OK` буцаана           | Алдааны хувь < 8%                                                           |
| Availability — серверийн эвдрэл | Сервер зогсоод сэргэх үеийн хүртээмж  | Сервер ажиллаж байгаад зогсоно | 20 VU, 2 минут               | Серверийг 10 секунд зогсоогоод дахин асаана | Асаасны дараа хүсэлтүүдэд дахин хариулна | Амжилттай хүсэлт / нийт хүсэлт > 90%; сэргэх хугацаа ойролцоогоор 10 секунд |


---

## SLO хүснэгт

| Сценарио | SLI (юуг хэмжих) | Босго | Цонх / нөхцөл |
|---|---|---|---|
| Performance | `/cart/add` latency | p95 < 5мс | 20 VU тогтмол ачаалалд, 1 мин |
| Reliability | `/pay` error rate | < 8% | 1 мин, 20 VU |
| Availability | бүх хүсэлтийн амжилтын хувь (checks) | > 90% | 2 мин, 10с зогсолт орсон |
| (Нэмэлт) Performance | `/report` latency | p95 < 450мс | 20 VU тогтмол ачаалалд, 1 мин |

**Босго сонгосон үндэслэл:**
- `/cart/add` p95 < 5мс — localhost дээр анхны туршилтаар бодит p95 ~2.46мс байсан тул 200мс гэдэг сул босго байх байсан, иймд бодит гүйцэтгэлдээ ойрхон, утга учиртай босго болгож 5мс болгосон.
- `/pay` error rate < 8% — серверт зориудаар ~5% алдаа суулгасан (`Math.random() < 0.05`) тул 8% нь бодитой, бага зэрэг хөвөгч зайтай босго.
- Availability ≥ 90% — 2 минутын турш 10 секундийн зогсолт орсныг тэсвэрлэх боломжтой түвшин гэж үзсэн.
- `/report` p95 < 450мс — сервер 200-400мс хооронд санамсаргүй хүлээдэг тул хамгийн муу тохиолдол дээр бага зэрэг зай үлдээж 450мс болгосон.

**Error budget тооцоо (цагаар):**
```
2 минутын цонхонд 90% availability = 12 секундийн "эвдрэх эрх"
(2 мин = 120с, 10% × 120с = 12с)
```

---

## PASS үр дүн — results/pass.txt

`slo-test.js`-ийг 20 VU, 1 минутын турш ажиллуулахад бүх 4 threshold амжилттай PASS гарлаа:

| Threshold | Босго | Бодит утга | Үр дүн |
|---|---|---|---|
| `checks` (availability) | rate > 0.90 | 98.20% | ✓ PASS |
| `http_req_duration{name:cart}` | p(95) < 5мс | 2.46мс | ✓ PASS |
| `http_req_duration{name:report}` | p(95) < 450мс | 389.56мс | ✓ PASS |
| `http_req_failed{name:pay}` | rate < 0.08 | 5.38% | ✓ PASS |

Нийт 2784 HTTP хүсэлт, 928 давталт, 20 VU-гаар 1мин 1.3с ажиллав. `/pay`-ийн 928 хүсэлтээс 50 нь (5.38%) 500 алдаа буцаасан — энэ нь серверт зориудаар суулгасан ~5%-ийн алдааны түвшинтэй нийцэж байна.

## Chaos туршилт — results/chaos.txt

`slo-test.js`-ийн `duration`-ыг `2m` болгож ажиллуулаад, 21 дэх секундэд серверийг зогсоож, дараа нь дахин асаав (лог дээр `connect: connection refused` алдаанууд 21:00:35–21:00:44 цагийн тэмдэглэгээнд бүртгэгдсэн.):

| Threshold | Босго | Бодит утга | Үр дүн |
|---|---|---|---|
| `checks` (availability) | rate > 0.90 | **89.10%** | ✗ **FAIL** |
| `http_req_duration{name:cart}` | p(95) < 5мс | 3.12мс | ✓ PASS |
| `http_req_duration{name:report}` | p(95) < 450мс | 391.8мс | ✓ PASS |
| `http_req_failed{name:pay}` | rate < 0.08 | **14.56%** | ✗ **FAIL** |

Нийт 5643 HTTP хүсэлт (2м01.3с ажиллав), үүнээс 615 (10.89%) амжилтгүй болсон. `checks` нийт 5643-аас 5028 амжилттай (89.10%), 615 амжилтгүй (10.89%). Endpoint тус бүрээр: cart 1715✓/166✗ (91%), report 1706✓/175✗ (90%), pay 1607✓/274✗ (85%, `/pay`-д зориулсан threshold дээр 14.56% error rate).

Console дэвсгэрт харагдсан эцсийн алдаа:
```
level=error msg="thresholds on metrics 'checks, http_req_failed{name:pay}' have been crossed"
```

**Availability threshold: ✗ FAIL** (89.10% < 90% босго)
**Reliability threshold (`http_req_failed{name:pay}`): ✗ FAIL** (14.56% > 8% босго)

**Бодит availability (хүсэлтээр тооцсон):**
```
availability = амжилттай checks / нийт checks = 5028 / 5643 = 89.10%
```
(`http_req_failed`-ээр тооцоход ч ойролцоо утга: 100% − 10.89% = 89.11% — хоёр аргаар тооцсон нь бараг давхцаж байна.)

**Яагаад цагаар тооцсон 12 секундийн budget ба хүсэлтээр тооцсон утга зөрдөг вэ:**

Цагаар: сервер ~10 секунд зогссон, нийт 121.3 секундийн турш ажилласан тул зогсолт нь **121.3с-ийн 8.24%**-ийг эзэлж байна — энэ нь миний тохируулсан 10%-ийн time-budget-аас **бага** тул цагаар харвал budget хэтрээгүй мэт харагдана.

Гэтэл бодит хүсэлтээр тооцоход алдааны хувь **10.89%** гарсан — өөрөөр хэлбэл time-budget-аас **их**. Шалтгаан нь: сервер хэвийн ажиллаж байхад `/report` endpoint дунджаар ~276-390мс хүлээдэг тул секунд тутамд цөөн хүсэлт л боловсруулагддаг. Харин сервер унасан үед (`connection refused`) хүсэлт **процесс хийлгэхгүйгээр агшин зуур** (0-1мс дотор) буцдаг тул зогсолтын секунд бүрд, хэвийн ажиллаж байсан үеийнхээс хамаагүй **олон** хүсэлт "багтаж" амжилтгүй болдог. Иймд зогсолтын 10 секунд бодит цагаараа тооцсоноос хамаагүй **олон хувийг** нийт хүсэлтийн тооноос "иддэг" — 8.24% цагийн зогсолт → 10.89% хүсэлтийн алдаа руу хөрвүүлэгдсэн нь яг энэ шалтгаанаас болсон.

**Reliability SLO мөн зөрчигдсөн үү:**

Тийм, `http_req_failed{name:pay}` мөн FAIL болсон (14.56% > 8%). Учир нь сервер унасан үед `/pay`-ийн хүсэлт бүр л амжилтгүй болдог — нэг эвдрэл (crash) нэг дор availability, reliability гэсэн хоёр SLO-г зэрэг зөрчдөг. Хоёр SLI-г бие даан хянахын тулд `/pay`-ийн алдааг зөвхөн serverr-ийн бизнес-логикоос (жишээ: `Math.random()<0.05` санамсаргүй алдаа) үүссэн 5xx-ийг тоолж, харин `connection refused`/timeout шалтгаантай алдааг тусад нь ангилж, зөвхөн availability SLI-д тооцох хэрэгтэй.

---

## FAIL үр дүн — results/fail.txt

`slo-test-fail.js`-д `/report`-ын threshold-ыг `p(95)<100`мс болгож зориуд эвдсэн (сервер 200мс-ээс хурдан хэзээ ч хариулдаггүй тул найдвартай FAIL гарна):

```
$ k6 run slo-test-fail.js 2>&1 | tee results/fail.txt; echo "exit=$?"
```
Үр дүн results/fail.txt -д хадгалагдсан.

FAIL threshold мөр: 
http_req_duration{name:report} ✗ 'p(95)<100' p(95)=392.01ms

Энд /report endpoint-ийн бодит p95 response time 392.01 ms байсан боловч threshold нь 100 ms-ээс бага гэж тохируулсан тул SLO threshold зөрчигдөж, k6 exit code 99-тэй дууссан.

---

## Дүгнэлт (8-10 өгүүлбэр)

Энэ лабораторийн ажлаар чанарын сценариог SLO болгон тодорхойлж, тэдгээрийг k6-ийн threshold хэлбэрээр хэмжиж болохыг ойлголоо. Сценарио → SLO → threshold шилжилтийн үед хамгийн хэцүү байсан хэсэг нь хэрэглэгчийн шаардлагыг яг ямар хэмжүүрээр илэрхийлэхээ сонгох байсан. Эхэндээ threshold-ийг хэт сул тавихгүйгээр бодит baseline үр дүнд тулгуурлан сонгох нь илүү зөв гэдгийг туршилтаар харлаа. Жишээлбэл, /cart/add endpoint-ийн baseline p95 нь 2.46мс байсан тул p95 < 5мс босгыг сонгосон. Chaos туршилтаар серверийг зориудаар зогсооход availability 89.10% болж, 90%-ийн SLO зөрчигдсөнөөр availability threshold бодит эвдрэлд мэдрэг болохыг баталсан. Мөн time-based error budget болон request-based availability хоёр ижил зүйл биш бөгөөд серверийн downtime-ийн үед хүсэлт боловсруулах хурд өөрчлөгддөгөөс шалтгаалан үр дүн зөрж болохыг ойлголоо. Сервер унах үед /pay-ийн connection refused алдаанууд нэмэгдсэнээр reliability-ийн 8%-ийн босго мөн зөрчигдсөн нь нэг серверийн эвдрэл олон төрлийн SLO-д зэрэг нөлөөлж болохыг харуулсан. Харин зориуд /report-ын threshold-ийг p95 < 100мс болгож эвдэхэд бодит p95 нь 392.01мс гарч, k6 exit code 99-өөр FAIL болсон нь threshold ажиллаж байгааг баталсан. Дараагийн удаа SLO тодорхойлохдоо availability болон reliability-ийн алдааны шалтгааныг илүү нарийн ангилж, бизнесийн алдаа болон серверийн хүртээмжийн алдааг тусдаа хэмжихээр төлөвлөнө. Энэ лаборатори нь SLO-г зөвхөн тоон босго биш, бодит системийн төлөв, ачаалал, эвдрэл болон хэмжилтийн нөхцөлтэй хамт тодорхойлох хэрэгтэйг ойлгууллаа.

---

## Ажиллуулах заавар

```bash
npm install
node server.js &
mkdir -p results
k6 run slo-test.js 2>&1 | tee results/pass.txt
k6 run slo-test-fail.js 2>&1 | tee results/fail.txt; echo "exit=$?"
```

## Файлын бүтэц

```
server.js            — локал API (/cart/add, /report, /pay)
slo-test.js          — SLO threshold-той k6 скрипт (PASS хувилбар)
slo-test-fail.js     — зориуд эвдсэн threshold-той k6 скрипт (FAIL хувилбар)
results/pass.txt     — бүх threshold PASS гаралт
results/chaos.txt    — chaos туршилтын бүтэн гаралт
results/fail.txt     — зориуд эвдсэн threshold-ийн гаралт, exit code-той
screenshots - баримт, нотолгоо
```